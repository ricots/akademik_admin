<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.akademik_admin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".fragment.matkul" />
        <activity android:name=".activity.update_jadwal" />
        <activity android:name=".fragment.add_dosen" />
        <activity android:name=".activity.add_matkul" />
        <activity android:name=".fragment.home" />
        <activity android:name=".fragment.absen" />
        <activity android:name=".fragment.ujian" />
        <activity android:name=".activity.daftar_ujian"></activity>
    </application>

</manifest>